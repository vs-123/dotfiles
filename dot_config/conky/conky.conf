-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'top_right',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = '#241F31',
    default_outline_color = '#241F31',
    default_shade_color = '#241F31',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=12',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    own_window_transparent = true,
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

-- Variables: https://conky.cc/variables
conky.text = [[
${color #241F31}$color ${alignc} ${uid_name 1000}@$nodename -- $sysname $kernel
$hr
${color #241F31}[RAM]$color $mem / $memmax -- $memperc% ${membar 4}
${color #241F31}[SWAP USAGE]$color $swap / $swapmax -- $swapperc% ${swapbar 4}
${color #241F31}[CPU USAGE]$color $cpu% ${cpubar 4}
${color #241F31}[PROCESSES]$color $processes  ${color #241F31}
$hr
${color #241F31}[FILE SYSTEM]
 / $color${fs_used /}/${fs_size /} ${fs_bar 6 /}
$hr
${color #241F31}NAME              PID     CPU%   MEM%
${color #241F31} ${top name 1} ${top pid 1} ${top cpu 1} ${top mem 1}
${color #241F31} ${top name 2} ${top pid 2} ${top cpu 2} ${top mem 2}
${color #241F31} ${top name 3} ${top pid 3} ${top cpu 3} ${top mem 3}
${color #241F31} ${top name 4} ${top pid 4} ${top cpu 4} ${top mem 4}
${color #241F31} ${top name 5} ${top pid 5} ${top cpu 5} ${top mem 5}
]]
